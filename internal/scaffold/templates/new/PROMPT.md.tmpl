# {{.Name}}: Implementation Prompt

You are implementing the `{{.Name}}` feature. The complete specification is
in SPEC.md in this directory.

## Before You Start

Ask the user:

1. **What programming language should I use?**
2. Any other setup this spec needs (credentials, endpoints, environment-specific
   values) — add these questions as you fill in SPEC.md.

## What to Build

Read SPEC.md, then implement the function(s) it defines under "Function
Signature Contract". Follow the algorithm and examples exactly.

## Validation

Test fixtures are in test_fixtures.json in this directory. Run your
implementation against every case and make sure it passes before considering
the work done.
